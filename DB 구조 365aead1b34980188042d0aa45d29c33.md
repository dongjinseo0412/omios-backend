# DB 구조

카테고리: 개발
파일형태: 문서
생성일: 2026년 5월 19일 오후 9:21

---

# OMIOS 데이터베이스 구조 정리 문서 (v0.1 기준)

## 1. 프로젝트 개요

OMIOS는 방일 한국인 관광객을 대상으로 지역별·취향별 일본 오미야게(기념품)를 추천하는 웹 서비스이다.

현재 데이터는 엑셀 기반으로 수집 및 관리되며, 이후 CSV Import를 통해 DB에 적재하는 구조를 기준으로 설계하였다.

초기에는 MVP 수준의 핵심 기능 구현을 우선하되, 이후 확장을 고려하여 일부 확장용 테이블 및 컬럼을 유지하였다.

---

# 2. 전체 ERD 설계 방향

## 설계 원칙

### 1) 코드 기반 PK 사용

모든 주요 테이블은 숫자 AUTO_INCREMENT 방식 대신 사람이 읽을 수 있는 코드 기반 PK를 사용한다.

예시:

- 상품: `PRD_001`
- 지역: `REG_001`
- 키워드: `KEY_001`
- 구매처: `LOC_001`

### 2) 엑셀 → CSV → DB 구조 기준

실제 데이터 수집은 엑셀에서 진행하며, DB 구조도 이를 기준으로 통일하였다.

### 3) 다대다 관계 적극 활용

상품은 다음과 같은 요소들과 다대다 관계를 가진다.

- 키워드
- 추천 대상
- 연령대
- 구매처

이를 위해 연결 테이블(Mapping Table)을 별도로 구성하였다.

### 4) 확장 고려

현재는 대표 링크만 사용하지만 향후:

- 다중 구매 링크
- 링크 클릭 통계
- 추천 알고리즘
- 인기순 정렬

등의 기능 확장을 고려하여 일부 테이블은 유지한다.

---

# 3. 테이블 구조

---

# 3-1. products

## 설명

상품 기본 정보를 저장하는 핵심 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| product_code | VARCHAR(20) PK | 상품 코드 |
| name_ko | VARCHAR(150) | 한국어 상품명 |
| name_jp | VARCHAR(150) | 일본어 상품명 |
| brand_name | VARCHAR(100) | 브랜드명 |
| primary_region_code | VARCHAR(20) FK | 대표 지역 코드 |
| price_range_code | VARCHAR(20) FK | 가격대 코드 |
| price | INT | 실제 가격 |
| description | TEXT | 상품 설명 |
| purchase_tip | VARCHAR(255) | 구매 팁 |
| is_region_limited | TINYINT(1) | 지역 한정 여부 |
| image_url | VARCHAR(500) | 이미지 URL |
| source_note | VARCHAR(500) | 대표 구매 링크 또는 출처 |
| is_active | TINYINT(1) | 활성 여부 |
| view_count | INT | 조회수 |
| link_click_count | INT | 링크 클릭수 |
| created_at | DATETIME | 생성일시 |
| updated_at | DATETIME | 수정일시 |

---

## 비고

- 현재는 일정상 대표 링크를 `source_note` 컬럼에 임시 저장한다.
- 추후 다중 링크 기능은 `product_links` 테이블을 통해 확장 가능하다.
- `popularity_score`는 저장값이 아니라 계산값으로 처리하는 방향을 고려한다.

---

# 3-2. regions

## 설명

상품 지역 정보를 저장하는 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| region_code | VARCHAR(20) PK | 지역 코드 |
| name_ko | VARCHAR(100) | 한국어 지역명 |
| name_jp | VARCHAR(100) | 일본어 지역명 |
| description | VARCHAR(255) | 지역 설명 |
| is_active | TINYINT(1) | 활성 여부 |
| created_at | DATETIME | 생성일시 |
| updated_at | DATETIME | 수정일시 |

---

## 비고

- 상위 지역 구조(parent_region)는 현재 사용하지 않아 제거하였다.
- 지도 좌표(map_x, map_y)는 현재 UI 구조상 필요하지 않아 제거하였다.

---

# 3-3. price_ranges

## 설명

가격대 필터링을 위한 기준 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| price_range_code | VARCHAR(20) PK | 가격대 코드 |
| label | VARCHAR(100) | 가격대 라벨 |
| min_price | INT | 최소 가격 |
| max_price | INT | 최대 가격 |
| sort_order | INT | 정렬 순서 |
| is_active | TINYINT(1) | 활성 여부 |
| created_at | DATETIME | 생성일시 |
| updated_at | DATETIME | 수정일시 |

---

# 3-4. keywords

## 설명

상품 태그 및 키워드 정보를 저장하는 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| keyword_code | VARCHAR(20) PK | 키워드 코드 |
| name | VARCHAR(100) | 키워드명 |
| category | VARCHAR(50) | 키워드 분류 |
| is_active | TINYINT(1) | 활성 여부 |
| created_at | DATETIME | 생성일시 |
| updated_at | DATETIME | 수정일시 |

---

## category 예시

| category | 의미 |
| --- | --- |
| type | 상품 유형 |
| taste | 맛/향 |
| mood | 분위기/감성 |

---

# 3-5. gift_targets

## 설명

추천 대상 정보를 저장하는 테이블.

예:

- 부모님
- 친구
- 연인
- 직장 동료

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| target_code | VARCHAR(20) PK | 추천 대상 코드 |
| name | VARCHAR(100) | 추천 대상명 |
| is_active | TINYINT(1) | 활성 여부 |
| created_at | DATETIME | 생성일시 |
| updated_at | DATETIME | 수정일시 |

---

# 3-6. age_groups

## 설명

추천 연령대 정보를 저장하는 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| age_group_code | VARCHAR(20) PK | 연령대 코드 |
| name | VARCHAR(100) | 연령대명 |
| min_age | INT | 최소 나이 |
| max_age | INT | 최대 나이 |
| is_active | TINYINT(1) | 활성 여부 |
| created_at | DATETIME | 생성일시 |
| updated_at | DATETIME | 수정일시 |

---

# 3-7. purchase_locations

## 설명

상품 구매 가능 위치 정보를 저장하는 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| location_code | VARCHAR(20) PK | 구매처 코드 |
| region_code | VARCHAR(20) FK | 지역 코드 |
| name | VARCHAR(150) | 구매처명 |
| location_type | VARCHAR(50) | 구매처 유형 |
| description | VARCHAR(255) | 구매처 설명 |
| address | VARCHAR(255) | 주소 |
| website_url | VARCHAR(500) | 웹사이트 |
| is_active | TINYINT(1) | 활성 여부 |
| created_at | DATETIME | 생성일시 |
| updated_at | DATETIME | 수정일시 |

---

## location_type 예시

- airport
- souvenir_shop
- department_store
- station_shop

---

# 3-8. product_keywords

## 설명

상품 ↔ 키워드 다대다 연결 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| product_code | VARCHAR(20) FK | 상품 코드 |
| keyword_code | VARCHAR(20) FK | 키워드 코드 |
| created_at | DATETIME | 생성일시 |

---

## PK

```sql
PRIMARY KEY (product_code, keyword_code)
```

---

# 3-9. product_targets

## 설명

상품 ↔ 추천 대상 다대다 연결 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| product_code | VARCHAR(20) FK | 상품 코드 |
| target_code | VARCHAR(20) FK | 추천 대상 코드 |
| created_at | DATETIME | 생성일시 |

---

## PK

```sql
PRIMARY KEY (product_code, target_code)
```

---

# 3-10. product_age_groups

## 설명

상품 ↔ 연령대 다대다 연결 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| product_code | VARCHAR(20) FK | 상품 코드 |
| age_group_code | VARCHAR(20) FK | 연령대 코드 |
| created_at | DATETIME | 생성일시 |

---

## PK

```sql
PRIMARY KEY (product_code, age_group_code)
```

---

# 3-11. product_purchase_locations

## 설명

상품 ↔ 구매처 다대다 연결 테이블.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| product_code | VARCHAR(20) FK | 상품 코드 |
| location_code | VARCHAR(20) FK | 구매처 코드 |
| is_active | TINYINT(1) | 판매 여부 |
| created_at | DATETIME | 생성일시 |

---

## PK

```sql
PRIMARY KEY (product_code, location_code)
```

---

## 비고

- 현재는 판매 여부를 단순 boolean 형태로 관리한다.
- 추후 필요 시 availability_status 형태로 확장 가능하다.

---

# 3-12. product_links

## 설명

상품 구매 링크 정보를 저장하는 확장용 테이블.

현재는 대표 링크만 사용하지만 향후 다중 링크 기능 확장을 고려하여 유지한다.

---

## 컬럼

| 컬럼명 | 타입 | 설명 |
| --- | --- | --- |
| link_code | VARCHAR(20) PK | 링크 코드 |
| product_code | VARCHAR(20) FK | 상품 코드 |
| link_type | VARCHAR(50) | 링크 유형 |
| site_name | VARCHAR(100) | 사이트명 |
| url | VARCHAR(500) | 링크 URL |
| is_primary | TINYINT(1) | 대표 링크 여부 |
| is_active | TINYINT(1) | 활성 여부 |
| created_at | DATETIME | 생성일시 |
| updated_at | DATETIME | 수정일시 |

---

## 비고

- 현재 실제 데이터는 일정상 `products.source_note` 컬럼에 대표 링크를 저장하고 있다.
- 추후 상품별 다중 구매 링크 관리 기능 확장을 고려하여 테이블 구조는 유지한다.

---

# 4. 정렬 및 통계 기능 관련

현재 고려 중인 기능:

- 가격순 정렬
- 인기순 정렬
- 많이 본 상품
- 많이 클릭된 상품

이를 위해 다음 컬럼을 유지한다.

- view_count
- link_click_count

단, `popularity_score`는 저장값보다는 계산값으로 처리하는 방향을 고려한다.

---

# 5. 향후 확장 가능 기능

## 1) 추천 알고리즘

- 조회수 기반 추천
- 클릭 기반 추천
- 키워드 기반 추천
- 사용자 선호 기반 추천

## 2) 다중 구매 링크

- 라쿠텐
- 아마존 재팬
- 공식몰
- 지역 쇼핑몰

## 3) 지역 기반 UI 확장

- 지도 기반 탐색
- 지역 계층 구조

## 4) 사용자 기능

- 찜하기
- 리뷰
- 평점
- 추천 기록

---

# 6. 최종 설계 방향 요약

현재 DB 구조는:

- 실제 데이터 수집 가능성
- 엑셀 관리 편의성
- CSV Import 효율성
- 프론트 필터/정렬 기능
- 향후 확장성

을 모두 고려하여 설계하였다.

특히 코드 기반 PK 구조를 통해:

- 데이터 가독성
- AI 기반 데이터 수집 자동화
- 수작업 검수 효율

을 높이는 방향으로 설계하였다.